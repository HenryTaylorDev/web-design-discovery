import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "'Roboto Mono', sans-serif", // For headings
    body: "'Roboto Mono', sans-serif", // For body text
  },
});

export default theme;
